# Regenerable build artifacts — frames alone were 3.5 GB
work/
__pycache__/
*.pyc
venv/
.venv/

# Finished renders: too big for git, and rebuildable from the pipeline
out/
*.mp4

# Keep posters out too
out/*.jpg

# Local voice model environment (1.7 GB, reinstall from requirements-voz.txt)
vendor/

# Your voice reference — personal biometric data, never commit
roteiros/voz_ref.*
roteiros/audio.*
roteiros/agenda.json
roteiros/estilos_usados.json
